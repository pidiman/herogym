# App
APP_PORT=8091

# Database (PostgreSQL)
DB_NAME=herogym
DB_USER=herogym
DB_PASS=changeme_strong

# Shared Adminer network
# The shared Adminer container must also be attached to this Docker network.
ADMINER_NETWORK=adminer
